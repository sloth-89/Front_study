// Math.ceil()
// 소수값이 존재할 때 값을 올리는 역할을 하는 함수

let val = 24.3;
console.log(Math.ceil(val));

// Math.floor()
// 소수값이 존재할 때 소수값을 버리는 역할을 하는 함수

let val2 = 24.3;
console.log(Math.floor(val2));

// Math.round()
// 소수값에 따라 올리거나 버리는 역할을 하는 반올림 함수

let val3 = 24.3;
console.log(Math.round(val3));